// AURAVIA 前端配置
// ──────────────────────────────────────────────
// 后端 API 基地址。规则：
//   - 以 "http" 开头 → 直接用（例如本地调试时填 "http://localhost:8000"）
//   - 留空 ""      → 使用相对路径（部署在同源后端上时，自动指向自己）
// 部署到 Render / Surge 等“前后端同源”场景时，保持空字符串即可。
window.AURAVIA_API_BASE = "";
